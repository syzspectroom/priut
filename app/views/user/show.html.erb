<%=@user.name%><br />
<%@user.dogs.each do |dog|%>
<%if !dog.slug.nil? %>
<%= link_to  dog.name, dog_path(dog.id)  %><br />
<% end %>
 <%= image_tag(dog.image.url(:thumb)) if dog.image? %>
<br />
<% end %>